<!--
  TEMPLATE: skill_md.template
  PRODUCED BY: skill-generator
  PURPOSE: Renders into `.claude/skills/{{skill_name}}/SKILL.md` — the entry
  point Claude Code reads when this skill's description matches the current
  task. This file must remain valid on its own; do not delete the frontmatter
  or the section headers below.
  RELATES TO: role.md (persona/boundaries), manifest.json (metadata),
  workflows/*.md (procedures this skill follows), prompts/*.md (reusable
  prompt scaffolds), examples/*.md (worked scenarios).
-->
---
name: {{skill_name}}
description: {{skill_description}}
compatibility: Claude Code project. Generated {{generated_date}} by skill-generator v{{generator_version}}.
---

# {{skill_title}}

<!-- TODO: one-paragraph summary of what this skill does and why it exists. -->

## When to use this skill

<!-- TODO: list concrete trigger phrases / situations. Be specific and a
     little "pushy" — skills under-trigger by default, so name the exact
     contexts where this skill should kick in. -->

## When NOT to use this skill

<!-- TODO: explicit non-goals / adjacent tasks this skill should NOT handle. -->

## Inputs

<!-- TODO: what does this skill need from the user or the environment
     before it can run? (files, parameters, prior context) -->

## Workflow

This skill follows the procedure defined in
`.claude/workflows/{{skill_name}}/workflow.md`. Read that file for the
authoritative step-by-step sequence before acting.

High-level summary:

1. <!-- TODO: step 1 -->
2. <!-- TODO: step 2 -->
3. <!-- TODO: step 3 -->

## Outputs

<!-- TODO: what files/artifacts/answers does this skill produce, and where
     do they go? -->

## Architecture constraints

- Never break existing project architecture; see
  `.claude/roles/{{skill_name}}/role.md` for the full boundary contract.
- Never move or rename existing files unless explicitly requested.
- Preserve backward compatibility of any public API this skill touches.

## Engineering Rules (auto-injected)

Every skill generated by skill-generator v2+ includes these four rule
categories automatically, regardless of domain. They are not optional and
should not be deleted when filling in the TODOs elsewhere in this file.

{{engineering_rules_block}}

## Project Context (auto-loaded at generation time)

skill-generator reads the following project context automatically before
generating this file. This section is a snapshot from generation time —
re-run the generator (or consult the live files directly) if the project
has since changed.

**Constitution / rules documents:**

{{context_report_block}}

**Architecture graph:**

{{graph_report_block}}

**Project structure snapshot:**

{{project_structure_block}}

## Validation

Before considering a task from this skill complete, check it against
`.claude/skills/{{skill_name}}/VALIDATION_CHECKLIST.md`.

## Related assets in this bundle

- Role contract: `.claude/roles/{{skill_name}}/role.md`
- Manifest: `.claude/manifests/{{skill_name}}/manifest.json`
- Prompt templates: `.claude/prompts/{{skill_name}}/`
- Workflow: `.claude/workflows/{{skill_name}}/workflow.md`
- Examples: `.claude/skills/{{skill_name}}/examples/`
- Validation checklist: `.claude/skills/{{skill_name}}/VALIDATION_CHECKLIST.md`
