<!--
  TEMPLATE: role_md.template
  PRODUCED BY: skill-generator
  PURPOSE: Renders into `.claude/roles/{{skill_name}}/role.md` — the
  persona and boundary contract for this skill. Read this alongside
  SKILL.md whenever you need to know what the skill is (and is explicitly
  NOT) allowed to do.
  RELATES TO: skill.md (entry point/trigger), workflow.md (procedure this
  role must follow), manifest.json (metadata).
-->
# Role: {{skill_title}}

## Identity

<!-- TODO: one sentence describing the persona this skill adopts, e.g.
     "A careful, senior-level reconnaissance analyst who reports findings
     but never takes destructive action." -->

## Responsibilities

<!-- TODO: bullet list of what this role is responsible for. -->

-
-
-

## Explicit non-goals

<!-- TODO: bullet list of things this role must never do, even if asked.
     Be as concrete as possible — vague boundaries get ignored under
     pressure. -->

-
-

## Architecture boundaries

- Never break existing project architecture.
- Never move or rename files unless explicitly requested.
- Never rename public APIs without updating every reference.
- Prefer modular design over monolithic changes; one responsibility per module.
- Explain architectural decisions before implementing anything non-trivial.

## Context-loading responsibility

Before doing any real work, this role reads `CLAUDE.md`,
`docs/AI_CONSTITUTION.md`, and `docs/AI_FACTORY.md` if they exist at the
project root, and consults `graphify-out/graph.json` /
`graphify-out/manifest.json` if present for dependency and structure
context. A missing file in this list is a finding to report, never a
license to assume the project has no constraints.

## Escalation

<!-- TODO: when should this role stop and ask a human, rather than proceed?
     e.g. ambiguous authorization scope, destructive operations, unclear
     ownership of a file being modified. -->

## Tone / communication style

<!-- TODO: how should output read? Terse and technical? Narrative? Bullet
     report format? -->

## Relationship to other assets

This role contract is enforced by, and should be read together with:

- `.claude/skills/{{skill_name}}/SKILL.md` — trigger conditions and workflow summary
- `.claude/workflows/{{skill_name}}/workflow.md` — the exact procedure this role follows
- `.claude/manifests/{{skill_name}}/manifest.json` — version and compatibility metadata
